{% block content %}
<!-- templates/painel.html -->
<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Painel AnaSync</title>
</head>
<body>
    <h1>Bem-vindo ao Painel AnaSync!</h1>
    <p>Aqui você pode gerenciar suas operações.</p>
    <a href="/publicar">Publicar um Post Agora</a>
</body>
</html>
<html>
<head>
    <title>Painel AnaSync</title>
</head>
<body>
    <h1>Painel da AnaSync</h1>
    <p>Status: Online</p>
    <a href="/publicar?senha=sua_senha_aqui">
        <button>Publicar Post</button>
    </a>
</body>
</html>
  <div class="jumbotron text-center">
        <h1>Bem-vindo ao Viral Video App!</h1>
        <p class="lead">Sua plataforma para compartilhar e descobrir vídeos virais.</p>
        {% if current_user.is_authenticated %}
            <p>Pronto para começar?</p>
            <a href="{{ url_for('video.my_videos_page') }}" class="btn btn-success btn-lg">Ver Vídeos</a>
            <a href="{{ url_for('video.upload_video_page') }}" class="btn btn-info btn-lg">Enviar Vídeo</a>
            <a href="{{ url_for('video.generate_video_page') }}" class="btn btn-primary btn-lg">Gerar Vídeo Automático</a> {# <-- ADICIONE ESTA LINHA #}
        {% else %}
            <p>Faça login ou cadastre-se para começar.</p>
            <a href="{{ url_for('auth.login') }}" class="btn btn-primary btn-lg">Login</a>
            <a href="{{ url_for('auth.register') }}" class="btn btn-secondary btn-lg">Cadastro</a>
        {% endif %}
    </div>
{% endblock %
